{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python" // Tells Vercel: Use the Python runtime on app.py
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py" // Routes all traffic to your app.py function
    }
  ]
}
